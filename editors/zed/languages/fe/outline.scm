; The outline lists procedures by their identifier — the name the host and
; `call` use. The crew-facing `name` string is deliberately not the outline
; entry: it is not what you search for when you are editing.

(procedure
  "procedure" @context
  name: (identifier) @name) @item
